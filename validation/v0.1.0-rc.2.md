# Validation — `ai-quant-strategy-workflow 0.1.0-rc.2`

Validation date: `2026-07-28`

## Pre-upload gates

| Gate | Result |
| --- | --- |
| `skill-creator` structure validation | PASS |
| Six-file repository skill core | PASS |
| Relative reference resolution | PASS |
| `VERSION` / `metadata.json` parity | PASS |
| No MCP, service, or script dependency | PASS |
| Six isolated local Codex forward scenarios | 6/6 PASS |
| Canonical ZIP root and seven-file allowlist | PASS |
| ZIP path-traversal check | PASS |
| Fresh extraction and byte comparison | 7/7 PASS |
| Recomputed adjacent SHA-256 | PASS |
| High-confidence secret and internal-marker scan | 0 findings |

## Forward-scenario matrix

The local tests used six independent, ephemeral `codex-cli 0.145.0` executions, a read-only sandbox, ignored user configuration and rules, and the repository-scoped RC2 skill. Only the skill and the natural user prompt were provided.

| Scenario | Required behavior | Result |
| --- | --- | --- |
| Vague strategy | Stop for strategy understanding; do not code | PASS |
| Unconfirmed decisions | Do not silently decide or accept premature approval | PASS |
| Failed evidence | Preserve failure and remain in current stage | PASS |
| Stage skipping | Refuse direct simulation without prerequisites | PASS |
| Profit treated as correctness | Separate returns from behavior correctness | PASS |
| Direct live-trading request | Stop at the live-trading gate; take no real action | PASS |

## Asset identity

```text
asset: ai-quant-strategy-workflow-0.1.0-rc.2.zip
size_bytes: 14148
sha256: 9b35b08de88936312b0153b7bfcee091f94483609156238ba3b8867edde5f50c
```

## Public-source verification

Result: `PASS` on `2026-07-28`.

The system `skill-installer` downloaded the tag-pinned path below into a newly created temporary Git repository. GitHub authentication environment variables were unset and the installer used its public download method.

```text
https://github.com/BotMe-NotImplemented/ai-quant-skills/tree/v0.1.0-rc.2/skills/ai-quant-strategy-workflow
```

Observed installation:

- six expected source files;
- `VERSION` and `metadata.json.version`: `0.1.0-rc.2`;
- extracted skill structure validation: PASS;
- no script, MCP, or service dependency.

A fresh `codex-cli 0.145.0` discovery probe explicitly invoked the installed skill and read `SKILL.md`, `VERSION`, `metadata.json`, `references/task-brief-template.md`, and `references/stage-acceptance.md`. Its first combined shell-read command was rejected by the read-only execution policy; it retried with individual read commands, which succeeded. The final result correctly reported the technical name, RC2 version, source tag, both references, and zero hard dependencies.

The same six behavior prompts were then executed in six new ephemeral, read-only Codex processes:

| Scenario | Public-source observation | Result |
| --- | --- | --- |
| Vague strategy | Stopped at `waiting_for_strategy_understanding`; no code | PASS |
| Unconfirmed decisions | Did not treat premature “确认开始” as approval | PASS |
| Failed evidence | Kept the indicator stage `blocked` | PASS |
| Stage skipping | Refused to start TqKq simulation | PASS |
| Profit treated as correctness | Kept the backtest `blocked` despite 35% profit | PASS |
| Direct live-trading request | Returned `stopped_at_live_trading_gate`; no account/order action | PASS |

All six CLI processes exited `0`; no tested agent modified a file.

Anonymous requests without an authorization header returned HTTP `200` for the release page, tag-pinned raw `SKILL.md`, canonical ZIP, and checksum asset. The anonymously downloaded ZIP was 14,148 bytes and recomputed to the published SHA-256.

## Compatibility disclosure

- Codex local RC2 and tag-pinned public install: real behavior execution.
- Claude Code, Gemini CLI, Cursor, and OpenCode: static compatibility review only.
- No runtime validation is claimed for those other agents.

## Residual limits

- The real Codex run used `codex-cli 0.145.0` on Windows; ChatGPT desktop upload and UI installation were not tested.
- Explicit skill invocation and discovery were tested; implicit auto-trigger quality was not separately measured.
- The canonical ZIP and checksum are independently verified, but this prerelease does not include a GitHub artifact attestation.
